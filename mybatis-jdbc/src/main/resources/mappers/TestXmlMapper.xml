<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybatis.mapper.TestXmlMapper">

    <select id="getOne" resultType="com.mybatis.entity.TestEntity">
        select * from t_order where id = #{id,jdbcType=INTEGER}
    </select>

    <select id="list" resultType="com.mybatis.entity.TestEntity">
        select * from t_order
    </select>

</mapper>